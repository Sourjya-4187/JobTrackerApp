# JobTrackerApp

Next.js 14 job tracker with localStorage persistence, an Add Job modal, a Job Description field, and a mobile-friendly responsive layout.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
